# Ship checkpoint — overlays-pip

Started: 2026-08-13
Current stage: done — all 7 stages complete

**Scope:** Phase 3b of MotionKit (final Phase 3 change): a scene-anchored `overlays[]` array on the Video Specification, with `pip` as its first overlay type — a video bubble anchored to a scene via `sceneIndex`, with `position`/`shape`/`size` (brand/schema-defaulted) and a required `audio: "own" | "muted"` mode. Adds a `pipStyle` brand token. Modifies `video-spec`/`video-validation`/`video-rendering`/`brand-system`. Closes out Phase 3 (Responsive Layout).

**Detected surfaces:** OpenSpec: yes · Pencil: no · Storybook: no · E2E: no (unchanged from prior phases)

| #   | Stage           | Status  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| :-- | :-------------- | :------ | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Frame           | done    | Change fully planned via /opsx:propose before this ship run started.                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1   | Design (Pencil) | skipped | No Pencil in this repo.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2   | Spec reconcile  | done    | No Pencil design to reconcile against; proposal/specs/design/tasks authored together, already pass `openspec validate`.                                                                                                                                                                                                                                                                                                                                                 |
| 3   | Build           | done    | Implemented directly in the main session (same approach as `responsive-motion`, given its reliability). All 18 tasks.md boxes checked; overlays[]/pip schema, pipStyle brand token, sceneIndex/asset/position validation, PIP bubble rendering, independent own-audio spans. One schema fix along the way: `sceneIndex` was accidentally `.nonnegative()` (structural), conflicting with the design's semantic-bounds-check decision — caught by a failing test, fixed. |
| 4   | Test            | done    | 174/174 tests passing (core 166, cli 7, mcp 1); lint + typecheck clean. New render tests include a corner-region probe distinguishing `circle` vs `rounded_square` shape, and a simultaneous-audio test for the deliberate no-ducking behavior. All three prior example specs confirmed unchanged; new `overlays-spec.json` added and manually smoke-tested (1080x1920, 7s, opened and watched).                                                                        |
| 5   | Sync            | done    | 4 modified main specs (video-spec, video-validation, video-rendering, brand-system); `openspec validate --specs` passes 5/5.                                                                                                                                                                                                                                                                                                                                            |
| 6   | Archive         | done    | Moved to openspec/changes/archive/2026-08-13-overlays-pip/. `openspec validate --specs` still passes 5/5.                                                                                                                                                                                                                                                                                                                                                               |

Status values: `pending` · `in-progress` · `done` · `skipped` (with reason).

## Decision log

Newest first. One line per meaningful decision or stage transition.

- 2026-08-13 — Archived the change to openspec/changes/archive/2026-08-13-overlays-pip/. Phase 3b (Overlays/PIP) shipped end to end — Phase 3 (Responsive Layout) is now fully complete.
- 2026-08-13 — Synced 4 deltas into openspec/specs/** as modified main specs; `openspec validate --specs` passes 5/5. Committed the implementation (acd6456). Entering stage 6 (Archive), gated on user confirmation.
- 2026-08-13 — Implementation complete (all 18 tasks checked off). Full suite green (174/174). Entering stage 5 (Sync), gated on user confirmation.
- 2026-08-13 — Checkpoint created. Change `overlays-pip` authored via `/opsx:propose` immediately before this ship run (proposal, 4 delta specs, design, tasks all written together) and already passes `openspec validate`. Stage 1 skipped (no Pencil), stage 2 has nothing to reconcile. Entering stage 3 (Build) directly in the main session (not a background agent) — the prior change (`responsive-motion`) hit two consecutive background-agent stalls during research-heavy phases; this change is smaller and touches the same, now-very-familiar files, so implementing directly is the more reliable path.
